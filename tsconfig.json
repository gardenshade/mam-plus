/*
* This file exists to stop vscode from throwing errors that are actually solved by
* settings inside `gulpfile.js`. Do not use this file for compiler options.
*/
{
    "include": [
        "src"
    ],
    "compilerOptions": {
        "target": "es6"
    }
}
